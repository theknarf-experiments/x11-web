services:
  sidecar:
    build:
      context: .
      dockerfile: Dockerfile.sidecar
    environment:
      BACKEND_URL: ws://host.docker.internal:3001/ws/sidecar
      SIDECAR_NAME: dev-sidecar
      DISPLAY_NUMBER: "99"
      RUST_LOG: info
