--- a/src/generator/namespace/parse.rs	2026-05-11 20:02:49.199260847 +0200
+++ b/src/generator/namespace/parse.rs	2026-05-11 20:02:49.203260871 +0200
@@ -5,6 +5,11 @@
     Output,
 };
 
+/// `endian = false` emits the native `T::try_parse(...)` form (matching
+/// upstream). `endian = true` emits `T::try_parse_endian(..., endian)`
+/// and `parse_list_endian` instead, with a free variable `endian` of
+/// type [`ByteOrder`] assumed to be in scope at the call site (the
+/// outer `fn try_parse_endian` signature provides it).
 pub(super) fn emit_field_parse(
     generator: &NamespaceGenerator<'_, '_>,
     field: &xcbdefs::FieldDef,
@@ -12,6 +17,7 @@
     from: &str,
     container: FieldContainer,
     out: &mut Output,
+    endian: bool,
 ) {
     match field {
         xcbdefs::FieldDef::Pad(pad_field) => {
@@ -46,7 +52,7 @@
                 out,
                 "let ({}, remaining) = {};",
                 rust_field_name,
-                emit_value_parse(generator, &normal_field.type_, from),
+                emit_value_parse(generator, &normal_field.type_, from, endian),
             );
         }
         xcbdefs::FieldDef::List(list_field) => {
@@ -112,21 +118,39 @@
             {
                 let rust_element_type =
                     generator.type_to_rust_type(list_field.element_type.type_.get_resolved());
-                outln!(
-                    out,
-                    "let ({}, remaining) = crate::x11_utils::parse_list::<{}>(remaining, \
-                     {}.try_to_usize()?)?;",
-                    rust_field_name,
-                    rust_element_type,
-                    expr_to_str(
-                        generator,
-                        list_field.length_expr.as_ref().unwrap(),
-                        to_rust_variable_name,
-                        false,
-                        None,
-                        true,
-                    ),
-                );
+                if endian {
+                    outln!(
+                        out,
+                        "let ({}, remaining) = crate::x11_utils::parse_list_endian::<{}>(remaining, \
+                         {}.try_to_usize()?, endian)?;",
+                        rust_field_name,
+                        rust_element_type,
+                        expr_to_str(
+                            generator,
+                            list_field.length_expr.as_ref().unwrap(),
+                            to_rust_variable_name,
+                            false,
+                            None,
+                            true,
+                        ),
+                    );
+                } else {
+                    outln!(
+                        out,
+                        "let ({}, remaining) = crate::x11_utils::parse_list::<{}>(remaining, \
+                         {}.try_to_usize()?)?;",
+                        rust_field_name,
+                        rust_element_type,
+                        expr_to_str(
+                            generator,
+                            list_field.length_expr.as_ref().unwrap(),
+                            to_rust_variable_name,
+                            false,
+                            None,
+                            true,
+                        ),
+                    );
+                }
             } else if let Some(list_len) = list_field.length() {
                 for i in 0..list_len {
                     let tmp_name = format!("{rust_field_name}_{i}");
@@ -135,7 +159,7 @@
                         "let ({}_{}, remaining) = {};",
                         rust_field_name,
                         i,
-                        emit_value_parse(generator, &list_field.element_type, from),
+                        emit_value_parse(generator, &list_field.element_type, from, endian),
                     );
                     emit_value_post_parse(&list_field.element_type, &tmp_name, out);
                 }
@@ -174,7 +198,7 @@
                     outln!(
                         out,
                         "let (v, new_remaining) = {};",
-                        emit_value_parse(generator, &list_field.element_type, from),
+                        emit_value_parse(generator, &list_field.element_type, from, endian),
                     );
                     emit_value_post_parse(&list_field.element_type, "v", out);
                     outln!(out, "remaining = new_remaining;");
@@ -202,13 +226,25 @@
                 }
                 parse_params.push(variable);
             }
-            outln!(
-                out,
-                "let ({}, remaining) = {}::try_parse({})?;",
-                rust_field_name,
-                switch_struct_name,
-                parse_params.join(", "),
-            );
+            if endian {
+                let mut endian_params = parse_params.clone();
+                endian_params.push(String::from("endian"));
+                outln!(
+                    out,
+                    "let ({}, remaining) = {}::try_parse_endian({})?;",
+                    rust_field_name,
+                    switch_struct_name,
+                    endian_params.join(", "),
+                );
+            } else {
+                outln!(
+                    out,
+                    "let ({}, remaining) = {}::try_parse({})?;",
+                    rust_field_name,
+                    switch_struct_name,
+                    parse_params.join(", "),
+                );
+            }
         }
         xcbdefs::FieldDef::Fd(fd_field) => {
             let rust_field_name = to_rust_variable_name(&fd_field.name);
@@ -250,7 +286,7 @@
                         out,
                         "let ({}, remaining) = {};",
                         rust_field_name,
-                        emit_value_parse(generator, &expr_ref.type_, from),
+                        emit_value_parse(generator, &expr_ref.type_, from, endian),
                     );
                 }
                 _ => {
@@ -260,7 +296,12 @@
                     assert_eq!(expr_ref.name, "odd_length");
                     outln!(
                         out,
-                        "let (odd_length, remaining) = bool::try_parse(remaining)?;"
+                        "let (odd_length, remaining) = {};",
+                        if endian {
+                            "bool::try_parse_endian(remaining, endian)?"
+                        } else {
+                            "bool::try_parse(remaining)?"
+                        },
                     );
                     // And there is a matching special case to use this in request parsing.
                 }
@@ -295,11 +336,22 @@
     generator: &NamespaceGenerator<'_, '_>,
     type_: &xcbdefs::FieldValueType,
     from: &str,
+    endian: bool,
 ) -> String {
     let type_type = type_.type_.get_resolved();
     let rust_type = generator.type_to_rust_type(type_type);
     let params = generator.get_type_parse_params(type_type, from);
-    format!("{}::try_parse({})?", rust_type, params.join(", "))
+    if endian {
+        let mut endian_params = params.clone();
+        endian_params.push(String::from("endian"));
+        format!(
+            "{}::try_parse_endian({})?",
+            rust_type,
+            endian_params.join(", "),
+        )
+    } else {
+        format!("{}::try_parse({})?", rust_type, params.join(", "))
+    }
 }
 
 fn emit_value_post_parse(type_: &xcbdefs::FieldValueType, var_name: &str, out: &mut Output) {
--- a/src/generator/namespace/struct_type.rs	2026-05-11 20:02:49.199260847 +0200
+++ b/src/generator/namespace/struct_type.rs	2026-05-11 20:02:49.203260871 +0200
@@ -141,6 +141,7 @@
                         "remaining",
                         FieldContainer::Other,
                         out,
+                        false,
                     );
                 }
                 for field in fields.iter() {
@@ -208,6 +209,127 @@
 
         outln!(out.indent(), "}}");
         outln!(out, "}}");
+
+        // Endian-aware parallel impl. Emitted for the trait-impl case
+        // (`impl TryParseEndian for X`) and for the inherent
+        // `pub fn try_parse(value, ext_params)` case (parallel inherent
+        // `pub fn try_parse_endian(value, ext_params, endian)`). FD
+        // types are skipped — server-side FD passing isn't on our path.
+        if !has_fds {
+            if !external_params.is_empty() {
+                outln!(out, "impl {} {{", name);
+                let p = external_params
+                    .iter()
+                    .map(|ext_param| {
+                        format!(
+                            "{}: {}",
+                            to_rust_variable_name(&ext_param.name),
+                            generator.type_to_rust_type(&ext_param.type_),
+                        )
+                    })
+                    .collect::<Vec<_>>();
+                outln!(out.indent(), "#[allow(dead_code)]");
+                outln!(
+                    out.indent(),
+                    "pub fn try_parse_endian({}: &[u8], {}, endian: ByteOrder) -> Result<(Self, &[u8]), ParseError> {{",
+                    input_name,
+                    p.join(", "),
+                );
+            } else {
+                outln!(out, "impl TryParseEndian for {} {{", name);
+                outln!(
+                    out.indent(),
+                    "fn try_parse_endian({}: &[u8], endian: ByteOrder) -> Result<(Self, &[u8]), ParseError> {{",
+                    input_name,
+                );
+            }
+            out.indented(|out| {
+                out.indented(|out| {
+                    // Always touch `endian` so structs whose fields are
+                    // all byte-arrays (and therefore endian-agnostic)
+                    // don't trigger `unused_variables` under the
+                    // crate-wide `#![deny(unused)]`.
+                    outln!(out, "let _ = endian;");
+                    if !matches!(parse_size_constraint, StructSizeConstraint::None) {
+                        outln!(out, "let remaining = initial_value;");
+                    }
+                    NamespaceGenerator::emit_let_value_for_dynamic_align(fields, out);
+                    for field in fields.iter() {
+                        parse::emit_field_parse(
+                            generator,
+                            field,
+                            switch_prefix,
+                            "remaining",
+                            FieldContainer::Other,
+                            out,
+                            true,
+                        );
+                    }
+                    for field in fields.iter() {
+                        if !field
+                            .name()
+                            .map(|field_name| deducible_fields.contains_key(field_name))
+                            .unwrap_or(false)
+                        {
+                            parse::emit_field_post_parse(field, out);
+                        }
+                    }
+                    let field_names = fields
+                        .iter()
+                        .filter_map(|field| {
+                            if generator.field_is_visible(field, &deducible_fields) {
+                                Some(to_rust_variable_name(field.name().unwrap()))
+                            } else {
+                                None
+                            }
+                        })
+                        .collect::<Vec<_>>();
+                    outln!(
+                        out,
+                        "let result = {} {{ {} }};",
+                        name,
+                        field_names.join(", ")
+                    );
+                    match parse_size_constraint {
+                        StructSizeConstraint::None => (),
+                        StructSizeConstraint::Fixed(fixed_size) => {
+                            outln!(out, "let _ = remaining;");
+                            outln!(out, "let remaining = initial_value.get({}..)", fixed_size);
+                            outln!(out.indent(), ".ok_or(ParseError::InsufficientData)?;");
+                        }
+                        StructSizeConstraint::EmbeddedLength { minimum } => {
+                            outln!(out, "let _ = remaining;");
+                            outln!(
+                                out,
+                                "let remaining = initial_value.get({} + length as usize * 4..)",
+                                minimum,
+                            );
+                            outln!(out.indent(), ".ok_or(ParseError::InsufficientData)?;");
+                        }
+                        StructSizeConstraint::LengthExpr(length_expr) => {
+                            outln!(
+                                out,
+                                "let length = {}.try_to_usize()?;",
+                                expr_to_str(
+                                    generator,
+                                    length_expr,
+                                    to_rust_variable_name,
+                                    false,
+                                    None,
+                                    true,
+                                ),
+                            );
+                            outln!(out, "let _ = remaining;");
+                            outln!(out, "let remaining = initial_value.get(length..)");
+                            outln!(out.indent(), ".ok_or(ParseError::InsufficientData)?;");
+                        }
+                    }
+                    outln!(out, "Ok((result, remaining))");
+                })
+            });
+            outln!(out.indent(), "}}");
+            outln!(out, "}}");
+        }
     }
 
     if generate_serialize {
@@ -428,6 +550,62 @@
                     },
                     "bytes",
                     out,
+                    false,
+                );
+            }
+        });
+        outln!(out, "}}");
+    });
+    outln!(out, "}}");
+
+    // Endian-aware parallel `SerializeEndian` impl. Mirrors the
+    // `serialize_into` path but threads `endian` through every
+    // primitive write. For fixed-size structs we don't emit a
+    // `Bytes`-returning array form — the trait only provides the
+    // `serialize_endian_into(&mut Vec, endian)` shape.
+    if external_params.is_empty() {
+        outln!(out, "impl SerializeEndian for {} {{", name);
+    } else {
+        outln!(out, "impl {} {{", name);
+    }
+    out.indented(|out| {
+        if external_params.is_empty() {
+            outln!(
+                out,
+                "fn serialize_endian_into(&self, bytes: &mut Vec<u8>, endian: ByteOrder) {{",
+            );
+        } else {
+            outln!(out, "#[allow(dead_code)]");
+            outln!(
+                out,
+                "pub fn serialize_endian_into(&self, bytes: &mut Vec<u8>{}, endian: ByteOrder) {{",
+                ext_params_arg_defs,
+            );
+        }
+        out.indented(|out| {
+            outln!(out, "let _ = endian;");
+            outln!(out, "bytes.reserve({});", size);
+            for field in fields.iter() {
+                if skip_length_field && field.name() == Some("length") {
+                    continue;
+                }
+                serialize::emit_field_serialize_into(
+                    generator,
+                    field,
+                    deducible_fields,
+                    |field_name| {
+                        let rust_field_name = to_rust_variable_name(field_name);
+                        if !deducible_fields.contains_key(field_name)
+                            && !external_params.iter().any(|param| param.name == field_name)
+                        {
+                            format!("self.{rust_field_name}")
+                        } else {
+                            rust_field_name
+                        }
+                    },
+                    "bytes",
+                    out,
+                    true,
                 );
             }
         });
@@ -520,6 +698,59 @@
                     },
                     "bytes",
                     out,
+                    false,
+                );
+            }
+        });
+        outln!(out, "}}");
+    });
+    outln!(out, "}}");
+
+    // Endian-aware parallel `SerializeEndian` impl for variable-size
+    // structs. Same body shape as `serialize_into`, threading `endian`
+    // through every primitive emission.
+    if external_params.is_empty() {
+        outln!(out, "impl SerializeEndian for {} {{", name);
+    } else {
+        outln!(out, "impl {} {{", name);
+    }
+    out.indented(|out| {
+        if external_params.is_empty() {
+            outln!(
+                out,
+                "fn serialize_endian_into(&self, bytes: &mut Vec<u8>, endian: ByteOrder) {{",
+            );
+        } else {
+            outln!(out, "#[allow(dead_code)]");
+            outln!(
+                out,
+                "pub fn serialize_endian_into(&self, bytes: &mut Vec<u8>{}, endian: ByteOrder) {{",
+                ext_params_arg_defs,
+            );
+        }
+        out.indented(|out| {
+            outln!(out, "let _ = endian;");
+            for field in fields.iter() {
+                if skip_length_field && field.name() == Some("length") {
+                    continue;
+                }
+                serialize::emit_field_serialize_into(
+                    generator,
+                    field,
+                    deducible_fields,
+                    |field_name| {
+                        let rust_field_name = to_rust_variable_name(field_name);
+                        if !deducible_fields.contains_key(field_name)
+                            && !external_params.iter().any(|param| param.name == field_name)
+                        {
+                            format!("self.{rust_field_name}")
+                        } else {
+                            rust_field_name
+                        }
+                    },
+                    "bytes",
+                    out,
+                    true,
                 );
             }
         });
--- a/src/generator/namespace/switch.rs	2026-05-11 20:02:49.199260847 +0200
+++ b/src/generator/namespace/switch.rs	2026-05-11 20:02:49.203260871 +0200
@@ -483,6 +483,7 @@
                                         "remaining",
                                         FieldContainer::Other,
                                         out,
+                                        false,
                                     );
                                 }
                                 for field in case_fields.iter() {
@@ -580,6 +581,7 @@
                                         "remaining",
                                         FieldContainer::Other,
                                         out,
+                                        false,
                                     );
                                 }
                                 for field in case_fields.iter() {
@@ -631,6 +633,54 @@
 
     outln!(out.indent(), "}}");
     outln!(out, "}}");
+
+    // Stub `try_parse_endian` for switch types: delegates to native
+    // `try_parse`. Switch types are concentrated in xinput and aren't on
+    // the hot path for MSB clients yet; threading endian through the
+    // case-by-case body emission is future work.
+    if !external_params.is_empty() {
+        outln!(out, "impl {} {{", name);
+        let p = external_params
+            .iter()
+            .map(|ext_param| {
+                format!(
+                    "{}: {}",
+                    to_rust_variable_name(&ext_param.name),
+                    generator.type_to_rust_type(&ext_param.type_),
+                )
+            })
+            .collect::<Vec<_>>();
+        let arg_names = external_params
+            .iter()
+            .map(|ext_param| to_rust_variable_name(&ext_param.name))
+            .collect::<Vec<_>>();
+        outln!(out.indent(), "#[allow(dead_code)]");
+        outln!(
+            out.indent(),
+            "fn try_parse_endian(value: &[u8], {}, _endian: ByteOrder) -> Result<(Self, &[u8]), ParseError> {{",
+            p.join(", "),
+        );
+        out.indented(|out| {
+            out.indented(|out| {
+                outln!(out, "Self::try_parse(value, {})", arg_names.join(", "));
+            });
+        });
+        outln!(out.indent(), "}}");
+        outln!(out, "}}");
+    } else {
+        outln!(out, "impl TryParseEndian for {} {{", name);
+        outln!(
+            out.indent(),
+            "fn try_parse_endian(value: &[u8], _endian: ByteOrder) -> Result<(Self, &[u8]), ParseError> {{",
+        );
+        out.indented(|out| {
+            out.indented(|out| {
+                outln!(out, "Self::try_parse(value)");
+            });
+        });
+        outln!(out.indent(), "}}");
+        outln!(out, "}}");
+    }
 }
 
 fn emit_fixed_size_switch_serialize(
@@ -755,6 +805,7 @@
                                     to_rust_variable_name,
                                     "bytes",
                                     out,
+                                    false,
                                 );
                             }
                         });
@@ -778,6 +829,47 @@
         outln!(out, "}}");
     });
     outln!(out, "}}");
+
+    // SerializeEndian stub: delegates to native serialize. Switch types
+    // aren't endian-threaded yet; correct for our current use cases (the
+    // affected ones live in xinput and aren't reached from MSB clients).
+    emit_switch_serialize_endian_stub(generator, &external_params, name, ext_params_arg_defs.as_str(), out);
+}
+
+fn emit_switch_serialize_endian_stub(
+    generator: &NamespaceGenerator<'_, '_>,
+    external_params: &[xcbdefs::ExternalParam],
+    name: &str,
+    ext_params_arg_defs: &str,
+    out: &mut Output,
+) {
+    let ext_params_call_args =
+        generator.ext_params_to_call_args(true, to_rust_variable_name, external_params);
+    if external_params.is_empty() {
+        outln!(out, "impl SerializeEndian for {} {{", name);
+    } else {
+        outln!(out, "impl {} {{", name);
+    }
+    out.indented(|out| {
+        if !external_params.is_empty() {
+            outln!(out, "#[allow(dead_code)]");
+            outln!(
+                out,
+                "pub fn serialize_endian_into(&self, bytes: &mut Vec<u8>{}, _endian: ByteOrder) {{",
+                ext_params_arg_defs,
+            );
+        } else {
+            outln!(
+                out,
+                "fn serialize_endian_into(&self, bytes: &mut Vec<u8>, _endian: ByteOrder) {{",
+            );
+        }
+        out.indented(|out| {
+            outln!(out, "self.serialize_into(bytes{});", ext_params_call_args);
+        });
+        outln!(out, "}}");
+    });
+    outln!(out, "}}");
 }
 
 fn emit_variable_size_switch_serialize(
@@ -893,6 +985,7 @@
                                         to_rust_variable_name,
                                         "bytes",
                                         out,
+                                        false,
                                     );
                                 }
                             });
@@ -966,6 +1059,7 @@
                                             to_rust_variable_name,
                                             "bytes",
                                             out,
+                                            false,
                                         );
                                     }
                                 });
@@ -997,6 +1091,9 @@
         outln!(out, "}}");
     });
     outln!(out, "}}");
+
+    // SerializeEndian stub for variable-size switch: native delegate.
+    emit_switch_serialize_endian_stub(generator, &external_params, name, ext_params_arg_defs.as_str(), out);
 }
 
 pub(super) fn generate_switch(
--- a/src/generator/namespace/header.rs	2026-05-11 20:02:49.199260847 +0200
+++ b/src/generator/namespace/header.rs	2026-05-11 20:02:49.203260871 +0200
@@ -74,7 +74,7 @@
     outln!(out, "#[allow(unused_imports)]");
     outln!(
         out,
-        "use crate::x11_utils::{{Request, RequestHeader, Serialize, TryParse, TryParseFd}};"
+        "use crate::x11_utils::{{Request, RequestHeader, Serialize, TryParse, TryParseFd, TryParseEndian, ByteOrder, SerializeEndian}};"
     );
     if mode == Mode::X11rb {
         outln!(out, "use std::io::IoSlice;");
--- a/src/generator/namespace/mod.rs	2026-05-11 20:02:49.199260847 +0200
+++ b/src/generator/namespace/mod.rs	2026-05-11 20:02:49.203260871 +0200
@@ -457,6 +457,7 @@
                             "remaining",
                             FieldContainer::Other,
                             out,
+                            false,
                         );
                         parse::emit_field_post_parse(field, out);
                         outln!(out, "let _ = remaining;");
@@ -481,6 +482,18 @@
             outln!(out, "}}");
         });
         outln!(out, "}}");
+        // The union wrapper stores raw bytes; SerializeEndian is endian-
+        // agnostic (caller decides how to interpret the inner bytes).
+        outln!(out, "impl SerializeEndian for {} {{", rust_name);
+        out.indented(|out| {
+            outln!(
+                out,
+                "fn serialize_endian_into(&self, bytes: &mut Vec<u8>, _endian: ByteOrder) {{",
+            );
+            outln!(out.indent(), "bytes.extend_from_slice(&self.0);");
+            outln!(out, "}}");
+        });
+        outln!(out, "}}");
         outln!(out, "impl TryParse for {} {{", rust_name);
         out.indented(|out| {
             outln!(
@@ -503,6 +516,19 @@
             outln!(out, "}}");
         });
         outln!(out, "}}");
+        // The union wrapper stores raw bytes; interpretation of its
+        // inner fields is the caller's job, so the endian-aware variant
+        // is endian-agnostic and just delegates.
+        outln!(out, "impl TryParseEndian for {} {{", rust_name);
+        out.indented(|out| {
+            outln!(
+                out,
+                "fn try_parse_endian(value: &[u8], _endian: ByteOrder) -> Result<(Self, &[u8]), ParseError> {{",
+            );
+            outln!(out.indent(), "Self::try_parse(value)");
+            outln!(out, "}}");
+        });
+        outln!(out, "}}");
 
         let mut seen_field_types = HashSet::new();
 
@@ -656,6 +682,17 @@
             outln!(out, "}}");
         });
         outln!(out, "}}");
+        // Event-struct wrapper holds 32 opaque bytes; endian-agnostic.
+        outln!(out, "impl SerializeEndian for {} {{", rust_name);
+        out.indented(|out| {
+            outln!(
+                out,
+                "fn serialize_endian_into(&self, bytes: &mut Vec<u8>, _endian: ByteOrder) {{",
+            );
+            outln!(out.indent(), "bytes.extend_from_slice(&self.0);");
+            outln!(out, "}}");
+        });
+        outln!(out, "}}");
 
         outln!(out, "impl TryParse for {} {{", rust_name);
         out.indented(|out| {
@@ -674,6 +711,19 @@
             outln!(out, "}}");
         });
         outln!(out, "}}");
+        // Event-struct wrappers hold a 32-byte opaque payload; endian-aware
+        // parse falls through to the native impl since interpretation of
+        // the inner bytes is the caller's job.
+        outln!(out, "impl TryParseEndian for {} {{", rust_name);
+        out.indented(|out| {
+            outln!(
+                out,
+                "fn try_parse_endian(value: &[u8], _endian: ByteOrder) -> Result<(Self, &[u8]), ParseError> {{",
+            );
+            outln!(out.indent(), "Self::try_parse(value)");
+            outln!(out, "}}");
+        });
+        outln!(out, "}}");
 
         let mut events_with_from = HashSet::new();
 
--- a/src/generator/namespace/request.rs	2026-05-11 20:02:49.199260847 +0200
+++ b/src/generator/namespace/request.rs	2026-05-11 20:02:49.203260871 +0200
@@ -681,6 +681,7 @@
                                         false,
                                         &bytes_name,
                                         tmp_out,
+                                        false,
                                     );
                                 });
                                 outln!(tmp_out, "}}");
@@ -997,6 +998,7 @@
                     from,
                     FieldContainer::Request(name.to_string()),
                     out,
+                    false,
                 );
                 if !field
                     .name()
--- a/src/generator/namespace/serialize.rs	2026-05-11 20:02:49.199260847 +0200
+++ b/src/generator/namespace/serialize.rs	2026-05-11 20:02:49.203260871 +0200
@@ -154,6 +154,9 @@
     }
 }
 
+/// `endian = false` emits `value.serialize_into(bytes)` (matching upstream).
+/// `endian = true` emits `value.serialize_endian_into(bytes, endian)` and
+/// expects a free `endian: ByteOrder` variable in scope at the call site.
 pub(super) fn emit_field_serialize_into(
     generator: &NamespaceGenerator<'_, '_>,
     field: &xcbdefs::FieldDef,
@@ -161,6 +164,7 @@
     mut wrap_field_ref: impl FnMut(&str) -> String,
     bytes_name: &str,
     out: &mut Output,
+    endian: bool,
 ) {
     emit_assert_for_field_serialize(generator, field, deducible_fields, &mut wrap_field_ref, out);
     match field {
@@ -196,6 +200,7 @@
                     true,
                     bytes_name,
                     out,
+                    endian,
                 );
             } else {
                 emit_value_serialize_into(
@@ -205,6 +210,7 @@
                     false,
                     bytes_name,
                     out,
+                    endian,
                 );
             }
         }
@@ -218,12 +224,21 @@
                     wrap_field_ref(&list_field.name),
                 );
             } else if parse::can_use_simple_list_parsing(generator, &list_field.element_type) {
-                outln!(
-                    out,
-                    "{}.serialize_into({});",
-                    wrap_field_ref(&list_field.name),
-                    bytes_name
-                );
+                if endian {
+                    outln!(
+                        out,
+                        "{}.serialize_endian_into({}, endian);",
+                        wrap_field_ref(&list_field.name),
+                        bytes_name,
+                    );
+                } else {
+                    outln!(
+                        out,
+                        "{}.serialize_into({});",
+                        wrap_field_ref(&list_field.name),
+                        bytes_name,
+                    );
+                }
             } else {
                 out!(
                     out,
@@ -249,6 +264,7 @@
                         false,
                         "bytes",
                         out,
+                        endian,
                     );
                 });
                 outln!(out, "}}");
@@ -266,13 +282,26 @@
                 },
                 &switch_field.external_params.borrow(),
             );
-            outln!(
-                out,
-                "{}.serialize_into({}{});",
-                wrap_field_ref(&switch_field.name),
-                bytes_name,
-                ext_params_args,
-            );
+            if endian {
+                // Switch types only have a stub `serialize_endian_into`
+                // delegating to native; the actual case bodies aren't
+                // endian-threaded yet.
+                outln!(
+                    out,
+                    "{}.serialize_endian_into({}{}, endian);",
+                    wrap_field_ref(&switch_field.name),
+                    bytes_name,
+                    ext_params_args,
+                );
+            } else {
+                outln!(
+                    out,
+                    "{}.serialize_into({}{});",
+                    wrap_field_ref(&switch_field.name),
+                    bytes_name,
+                    ext_params_args,
+                );
+            }
         }
         xcbdefs::FieldDef::Expr(xcbdefs::ExprField {
             name,
@@ -300,7 +329,15 @@
                 rbytes_name,
                 field.list_name
             );
-            outln!(out, "let {0} = {0}.to_ne_bytes();", rbytes_name);
+            if endian {
+                outln!(
+                    out,
+                    "let {0} = match endian {{ ByteOrder::Lsb => {0}.to_le_bytes(), ByteOrder::Msb => {0}.to_be_bytes() }};",
+                    rbytes_name,
+                );
+            } else {
+                outln!(out, "let {0} = {0}.to_ne_bytes();", rbytes_name);
+            }
             outln!(out, "{}.extend_from_slice(&{});", bytes_name, rbytes_name);
         }
         field => unreachable!("Unknown field: {:?}", field),
@@ -442,6 +479,7 @@
     was_deduced: bool,
     bytes_var: &str,
     out: &mut Output,
+    endian: bool,
 ) {
     // Deduced fields are not converted to their enum value
     if let (false, Some(enum_def)) = (was_deduced, generator.use_enum_type_in_field(type_)) {
@@ -451,12 +489,31 @@
         let current_wire_size = type_.type_.get_resolved().size().unwrap();
 
         if max_wire_size > 1 && u32::from(max_wire_size / 8) > current_wire_size {
+            if endian {
+                outln!(
+                    out,
+                    "(u{}::from({}) as {}).serialize_endian_into({}, endian);",
+                    max_wire_size,
+                    value,
+                    rust_wire_type,
+                    bytes_var,
+                );
+            } else {
+                outln!(
+                    out,
+                    "(u{}::from({}) as {}).serialize_into({});",
+                    max_wire_size,
+                    value,
+                    rust_wire_type,
+                    bytes_var,
+                );
+            }
+        } else if endian {
             outln!(
                 out,
-                "(u{}::from({}) as {}).serialize_into({});",
-                max_wire_size,
-                value,
+                "{}::from({}).serialize_endian_into({}, endian);",
                 rust_wire_type,
+                value,
                 bytes_var,
             );
         } else {
@@ -477,20 +534,30 @@
             let item = s.upgrade().expect("not resolved?");
             let externals = item.external_params.borrow();
             if !externals.is_empty() {
-                // we have external paramaters
-                // normally we'd need to use wrap_field_ref here, but we don't need to
-                // since this is only used in one case IIRC
                 let ext_args = generator.ext_params_to_call_args(
                     true,
                     |name| format!("self.{}", to_rust_variable_name(name)),
                     &externals,
                 );
-                outln!(out, "{}.serialize_into({}{});", value, bytes_var, ext_args,);
-
+                if endian {
+                    outln!(
+                        out,
+                        "{}.serialize_endian_into({}{}, endian);",
+                        value,
+                        bytes_var,
+                        ext_args,
+                    );
+                } else {
+                    outln!(out, "{}.serialize_into({}{});", value, bytes_var, ext_args);
+                }
                 return;
             }
         }
 
-        outln!(out, "{}.serialize_into({});", value, bytes_var);
+        if endian {
+            outln!(out, "{}.serialize_endian_into({}, endian);", value, bytes_var);
+        } else {
+            outln!(out, "{}.serialize_into({});", value, bytes_var);
+        }
     }
 }
