// Storybook 10.3+ auto-applies preview annotations through the
// `@storybook/addon-vitest` plugin, so this file just exists as
// the conventional setup-file hook. Add per-test setup here if we
// ever need any (custom matchers, fakes, etc.).
export {};
